<?xml version="1.0" encoding="UTF-8" ?>
 <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="friendns">
		
	
	<!-- add friend -->
	<insert id="add_friend" parameterType="Map">
		insert into friendship values(friend_seq.NEXTVAL,#{email1}, #{email2})
	</insert>
	
	<!-- check friend -->
	<select id="check_friend" parameterType="Map" resultType="friend">
		select * from friendship where email1=#{email1} and email2=#{email2}
	</select>
	
	<!-- friend list  -->
	<select id="list" parameterType="String" resultType="friend">
		select * from friendship where email1=#{email1}
	</select>
	
	<!-- delete friend -->
	<delete id="del_friend" parameterType="friend">
		delete from friendship where fno=#{fno} and email1=#{email1}
	</delete>
	
</mapper>
